output {
  if [program] =~ /^postfix/ {
    redis {
      host => "localhost"
      data_type => "list"
      key => "postfix"
    }
  } else {
    redis {
      host => "localhost"
      data_type => "list"
      key => "forwarder"
    }
  }
}
